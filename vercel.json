{
  "installCommand": "pnpm install --filter @monkeytype/frontend... --ignore-scripts",
  "buildCommand": "pnpm build-fe",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
